{
  "name": "browserify-visualizer-example",
  "version": "0.0.1",
  "description": "shows how to use the visualizer with browserify",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js --outfile bundle.js",
    "watch": "watchify index.js -v -o bundle.js --verbose"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "watchify": "^3.9.0"
  }
}
